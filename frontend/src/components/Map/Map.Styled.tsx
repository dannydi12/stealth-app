import styled from 'styled-components'

const StyledMap = styled.div`

min-height: 100%;

.map-container {
    position: absolute;
    top: 0;
    bottom: 0;
    width: 100%;
}
`

export default StyledMap
